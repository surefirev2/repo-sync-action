# Fixture: literal repos with repo_include_paths overrides
repositories:
  - repo-a
  - repo-b

include_paths:
  - .github/workflows/sync.yaml

repo_include_paths:
  repo-b:
    - src/*

exclude_paths: []
